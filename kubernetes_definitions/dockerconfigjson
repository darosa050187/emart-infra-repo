AWS_ACCESS_KEY_ID=<add-access-key>
AWS_SECRET_ACCESS_KEY=<add-secret-access>
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=084828572941
ECR_REGISTRY="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"

AUTH_STRING=$(echo -n "AWS:${AWS_SECRET_ACCESS_KEY}" | base64)

cat <<EOF > .dockerconfigjson
{
  "auths": {
    "${ECR_REGISTRY}": {
      "username": "AWS",
      "password": "${AWS_SECRET_ACCESS_KEY}",
      "email": "danyboyvonderrose@gmail.com",
      "auth": "${AWS_SECRET_ACCESS_KEY}"
    }
  }
}
EOF
